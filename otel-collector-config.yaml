receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  otlp/grafana:
    endpoint: otlp-gateway-prod-sa-east-1.grafana.net:443
    headers:
      Authorization: ${OTEL_EXPORTER_OTLP_HEADERS}

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp/grafana]
